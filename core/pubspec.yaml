name: core
description: A new Flutter package project.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter
  get_it: ^8.0.3
  connectivity_plus: ^6.0.3
  google_fonts: ^6.2.1

flutter:
  generate: true